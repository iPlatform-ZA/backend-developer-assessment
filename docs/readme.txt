Tools Used

Entity Framework 6.x : Data Access and storage
Unity : Dependency Injection
xUnit :Unit Tests

Additional Info.

The database is created through the implemetation of DbInitializer for entity framework and the table populated 
when the seed method of the initializer is called by reading the contents of the SQL script 
file which contains insrt statements
